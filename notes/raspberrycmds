sudo shutdown -h now
sudo raspi-config (user koennen auto-login'd werden)
// epiphany browser ist speziell fuer raspberrypi zugeschnitten, allerdings helle pixelzeile oben. daher umgesteigen auf chromium.
df (-H): zeigt speicherplatz an (verfuegbar/belegt)
wenn raspberry hardwareclock hat (externes device), dann kann command hwclock sinn machen
startx started die GUI (nachdem user nach bootvorgang angemeldet ist)
